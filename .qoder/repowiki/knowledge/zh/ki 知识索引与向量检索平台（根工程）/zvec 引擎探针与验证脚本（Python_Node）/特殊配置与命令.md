Python 探针：`pip install zvec`（无 wheel 时需从源码 `git clone --recurse-submodules https://github.com/alibaba/zvec.git && cd zvec && pip install .`），然后 `python zvec_demo.py`；Node 探针：`cd zvec-probe-node && npm install`，再 `node zvec_demo.mjs`。运行会在系统临时目录创建带 PID 的 collection 目录并在结束时清理。