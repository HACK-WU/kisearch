运行 E2E 前需执行 `.e2e-run/env.sh` 以设置 Node 22 路径、`SILICONFLOW_API_KEY` 和 `KI_CFG=/root/knowledge-indexer/.e2e-run/ki-config.yaml`；单元测试无需外部依赖，但向量化相关用例需要 `SILICONFLOW_API_KEY` 或 `GITNEXUS_EMBEDDING_API_KEY` 环境变量，否则走 fail-loud 跳过；所有测试数据均写入 `/tmp/ki-test-*` 或 `.e2e-run/*` 隔离目录，不会污染 `~/.ki`。