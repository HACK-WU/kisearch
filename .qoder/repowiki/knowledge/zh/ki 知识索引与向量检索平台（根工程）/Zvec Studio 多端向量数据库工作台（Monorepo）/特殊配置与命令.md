根目录 Makefile 提供统一入口：`make install` 安装 Node + Python 依赖，`make dev` 并发启动后端与前端，`make build.pip` 将前端 dist 注入后端静态目录并打包 wheel，`make package.sidecar` 用 PyInstaller 冻结 sidecar，`make package.desktop` 用 Tauri 生成安装包，`make release-check` 串联 verify + verify.desktop + e2e 作为发布门禁。