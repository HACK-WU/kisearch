PyInstaller 冻结 FastAPI sidecar；Tauri（Rust）桌面打包；GitHub Actions + actions/checkout/setup-python/setup-node/pnpm-action-setup/dtolnay/rust-toolchain；pytest + coverage、vitest、cargo test/clippy/fmt；Playwright E2E；pip-audit / pnpm audit / cargo-audit 安全扫描；twine 发布 PyPI。