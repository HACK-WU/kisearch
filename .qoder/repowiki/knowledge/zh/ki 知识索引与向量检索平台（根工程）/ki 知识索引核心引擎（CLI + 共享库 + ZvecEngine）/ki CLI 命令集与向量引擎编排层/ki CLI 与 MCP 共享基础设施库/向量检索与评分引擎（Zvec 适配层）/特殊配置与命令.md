构建产物 `dist/zvec-engine/index.js` 是运行时必需（worker_threads 无法直接加载源码），源码变更后需执行 `npx tsc -p tsconfig.src.json`（npm run build）重新编译；向量库目录由 `getVectorDir(config)` 决定，损坏或锁冲突时可执行 `ki rebuild-vector` 或 `ki restore <scope> --from-snapshot --rebuild-vector` 恢复。