`ki mcp --http` 默认监听 `127.0.0.1:7423` 且回环绑定免鉴权；远程暴露需显式 `--host 0.0.0.0 --token <t>`。`startHttpMcpServer` 启动前会探测同 host:port 的健康实例以复用（幂等单例）。`--web` 标志启用静态页面服务（默认 `web/dist/index.html`，由 `KI_WEB_DIR` 环境变量覆盖）。