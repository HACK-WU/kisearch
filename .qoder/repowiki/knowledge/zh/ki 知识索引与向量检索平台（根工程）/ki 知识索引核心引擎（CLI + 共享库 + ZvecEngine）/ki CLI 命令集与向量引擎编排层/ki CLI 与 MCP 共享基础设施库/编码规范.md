- 配置统一通过 config.ts 的 loadConfig() 获取进程内缓存的 KiConfig，避免重复解析。
- Scope 参数先经 scope.ts::validateScope 校验字符集，再结合 config.scopeMode 做 default/strict 模式解析。
- 写操作统一经 preflight.ts 磁盘/权限预检 + Store/WAL 原子写入，读操作经 scope 校验后访问 group-index.json。
- 向量相关入口（batch-vectorize/path-vectorize/rebuild-vector/scoring）均基于同一 ZvecEngine 实例并共用 DEFAULT_PARTITION_CONFIG 冷热分区策略。
- 控制台交互统一使用 progress.ts 的进度输出，避免分散的 console.log。
- MCP 工具与 CLI 命令共享同一组 scope/group/store 类型定义，保证两端契约一致。