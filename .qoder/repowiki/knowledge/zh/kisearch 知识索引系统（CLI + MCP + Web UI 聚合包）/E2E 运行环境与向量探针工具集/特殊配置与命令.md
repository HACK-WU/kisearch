运行前需先执行 `source .e2e-run/env.sh` 以设置 Node v22 路径、`SILICONFLOW_API_KEY` 与 `KI_CFG`；MCP 测试脚本依赖 `/root/.ki/mcp-token` 与已在 `http://127.0.0.1:7423` 运行的 ki 服务；Python 探针依赖 `/root/bk-monitor/ai-docs/**/*.md` 作为批量文档源；Node 探针需先在 `zvec-probe-node/` 下执行 `npm install` 安装 `@zvec/zvec`。