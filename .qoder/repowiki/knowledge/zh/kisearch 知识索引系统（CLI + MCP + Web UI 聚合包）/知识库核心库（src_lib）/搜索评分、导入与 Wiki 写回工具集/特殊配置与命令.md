导入前需确保 scope 已初始化（存在 `relations-cache.json`），否则抛出错误；导入过程通过 `import.lock` 文件实现同 scope 互斥，SIGINT/SIGTERM 会写入中断标记并在退出时清理锁；Wiki 写回可通过 `wikiSync.enabled=false` 全局禁用，或通过 `wikiSync.autoBackfill=false` 关闭自动补齐历史文件。