- 所有子命令与 MCP 工具统一通过 commander 注册，参数使用 zod schema 校验后交由 core_lib 处理
- 数据写入采用 WAL 模式（临时文件 + 原子 rename），保证 JSON 文件一致性
- scope 作为物理隔离键，KB、向量库、备份均按 scope 分目录存放，读取前进行路径白名单校验
- 测试统一使用 jiti 直接执行 .test.ts，E2E 使用 node --test 运行 .mjs 用例
- 配置集中存放在 ~/.ki/config.yaml，通过 ki config init 生成，doctor 命令校验 apiKey、维度与目录