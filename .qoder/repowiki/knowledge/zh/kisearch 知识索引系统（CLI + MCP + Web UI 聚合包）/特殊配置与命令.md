npm install && npm run build:zvec-engine（必须，否则 ki mcp 启动报找不到 dist/zvec-engine/index.js）；npm test / npm run test:all 运行 jiti 驱动的单元测试；npm run test:zvec-engine 运行 node --test 的 ZvecEngine 测试；npx jiti src/<cmd>.ts 直接执行任意源码命令；ki mcp --http --web 启动 HTTP 单例并附带前端。