- 每个测试文件顶部用 JSDoc 注释说明覆盖的需求编号（如 S-01、REQ-04）与测试目标，便于追溯需求实现。
- 通过 `test/test-config.ts` 的 `registerTestScope` + `getTestEnv` 为子进程注入隔离的 `KI_CONFIG_PATH` 临时配置，避免污染用户 `~/.ki`。
- E2E 测试统一通过 `execFileSync('npx', ['jiti', 'src/xxx.ts', ...], { env: getTestEnv() })` 调用 CLI，并以 JSON.parse(stdout) 断言结构化输出。
- 向量/引擎类测试使用 `mkdtempSync(join(tmpdir(), '<prefix>-'))` 创建临时数据库目录，并在 `t.after` 中递归清理。
- 对外部依赖（embedding、memory 服务）采用内联 mock 工厂函数（如 `hashVector`、`buildTestServer`）注入，避免真实网络调用。
- 测试数据通过 `test/fixtures/mock-wiki/` 中的 git 仓库提供，增量测试通过在该仓库内新增/修改/删除文件并提交来驱动 diff。