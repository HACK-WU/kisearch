- embedding 采用轻量词哈希 bag-of-words + L2 归一化生成固定维度（DIM=256）稠密向量，仅用于验证流程而非生产质量。
- 混合检索统一通过 RRF（rankConstant=60）在进程内融合稠密向量与 BM25 全文两路召回，以规避单 Query 中 fts/vector 互斥的限制。
- Schema 统一声明 tag（INVERT 索引）、scope（普通 STRING）、content（FTS 索引）三个标量字段以及 dense（HNSW COSINE）向量字段。
- 集合生命周期遵循 create_and_open → insert → optimize → query → close/open 复用模式，并显式计时各阶段耗时用于冷启动性能评估。
- 测试数据固定为 8 条关系/搜索/路径样本，按 (id, tag, scope, content) 四元组组织，便于跨语言探针对齐行为。