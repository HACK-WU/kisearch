配置文件默认位于 ~/.ki/config.yaml（优先于 config.yml 与 config.json），可通过 --config 或 KI_CONFIG_PATH 指定；向量库目录默认 ~/.ki/vector，集合名为 kisearch；MCP HTTP 服务默认监听 127.0.0.1:7423，通过 ~/.ki/mcp-http.lock 做幂等单例；--web 模式需 web/dist/index.html 存在才提供前端页面。