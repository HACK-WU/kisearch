- 每个子模块以单一职责文件组织，CLI 命令与 MCP 工具通过 import 直接复用而非重复实现。
- 写操作统一经 preflight.ts 预检 + Store/WAL 原子写入，读操作经 scope 校验后访问 group-index.json。
- 向量相关入口（batch/vectorize/rebuild/scoring）均基于同一 ZvecEngine 实例并共用 scoring 冷热分区策略。
- 控制台交互统一使用 progress.ts 的进度输出，避免分散的 console.log。
- MCP 工具与 CLI 命令共享同一组 scope/group/store 类型定义，保证两端契约一致。