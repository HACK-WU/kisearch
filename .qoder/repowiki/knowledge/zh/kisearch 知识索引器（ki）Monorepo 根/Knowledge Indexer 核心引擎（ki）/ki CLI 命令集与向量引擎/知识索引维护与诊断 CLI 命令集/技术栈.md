Node.js CLI 使用 commander 进行参数解析；持久化通过 `lib/store` 读写 JSON 文件；向量操作统一经 `lib/vector-client` 的 `vectorSearch/vectorDelete/ensureVectorAvailable/closeEngine` 访问；评分与分区复用 `lib/scoring` 与 `lib/constants.DEFAULT_PARTITION_CONFIG`。