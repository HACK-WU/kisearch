- 所有 CLI 子命令以独立 TypeScript 文件置于 src/ 下，由 bin/ki.mjs 的命令映射表统一注册与转发
- 配置通过环境变量 KI_CONFIG_PATH 注入，子命令内部按 scope 隔离 KB 与向量索引空间
- MCP 服务统一以 stdio 或 HTTP 单例形式暴露，web 前端通过 /api 与 /mcp 路由访问
- 测试按单元/集成/e2e 分层组织，e2e 使用 node --test 执行并依赖 .env.e2e.example 中的凭据