开发：`npm run dev` 启动 Vite 在 127.0.0.1:5173，自动将 `/api`、`/mcp`、`/healthz` 代理到本地 `http://127.0.0.1:7423`（需先运行 `ki mcp --http`）；构建：`npm run build` 先执行 `tsc --noEmit` 类型检查再 `vite build` 产出 `web/dist`；预览：`npm run preview`。