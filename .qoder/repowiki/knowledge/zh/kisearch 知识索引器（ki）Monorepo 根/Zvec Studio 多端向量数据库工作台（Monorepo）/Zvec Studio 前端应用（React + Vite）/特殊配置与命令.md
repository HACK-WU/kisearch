`pnpm dev` 启动 Vite 开发服务器；`pnpm build` 先执行 tsc --noEmit 再做 vite build；`pnpm gen:api` 从本地后端 http://127.0.0.1:7860/api/v1/openapi.json 重新生成 `../../packages/api-client/src/schema.ts`；`pnpm test` / `pnpm e2e` 分别运行 Vitest 单元测试与 Playwright E2E。