Tauri v2 (tauri 2 + tauri-plugin-dialog) + Rust 2021 edition (rust-version=1.77)，Python sidecar 为同仓库 `apps/backend` 中的 FastAPI 应用，打包依赖 PyInstaller 冻结 sidecar 二进制；前端通过 `@tauri-apps/cli` 驱动，E2E 使用 WebdriverIO + Mocha。