- 所有跨路由共享的状态（Settings、Registry、AIService、ConfigStore）统一挂在 request.app.state，并由 deps.py 的 get_*_dep 函数读取，便于测试覆盖。
- 路由文件采用单一职责拆分（collections/documents/fs/searches/ai），全部通过 include_router(prefix=effective.api_prefix) 集中挂载。
- 请求/响应数据模型集中在 schemas/ 下以 Pydantic 定义，路由层不内联 dict 构造，保持 API 契约与实现分离。
- 自定义业务异常继承自 exceptions.ZvecStudioError，并在 middleware/error_handler.py 中统一转换为 application/problem+json 响应。
- AI 嵌入/重排器通过 AIFunctionRegistry 持久化注册，启动时由 _seed_builtins 自动写入内置条目，用户扩展与内置项走同一 CRUD 接口。