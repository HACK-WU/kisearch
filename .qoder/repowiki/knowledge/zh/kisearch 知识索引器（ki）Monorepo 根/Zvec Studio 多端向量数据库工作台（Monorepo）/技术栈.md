pnpm workspace + Makefile 作为跨语言编排层；FastAPI + Pydantic 定义 OpenAPI 契约；openapi-typescript 生成前端类型；Tauri v2 (Rust) 封装 Python sidecar 与 React SPA；pytest/ruff/mypy 覆盖后端，eslint/vitest/playwright 覆盖前端，cargo clippy 覆盖桌面壳。