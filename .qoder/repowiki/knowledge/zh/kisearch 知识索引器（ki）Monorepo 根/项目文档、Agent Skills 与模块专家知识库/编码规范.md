- SKILL.md 统一以 YAML frontmatter 声明 `name` 与 `description`，便于 Skill Loader 解析与展示。
- `.module-experts/` 下每个专家目录遵循统一的七层结构（契约层 C0-C4 + 实现层 implementation/01-07），并在 INDEX.md 中以关键词、源码根、测试状态元信息描述。
- 文档采用中文为主、英文术语保留原样的写法，技术名词（如 ki_search、MCP、VectorAdapter、Group/Relation）直接使用代码中的标识符而非翻译。
- Agent 行为类文档（如 ki-search）以流程图/速览表开头，再分步骤展开，并附带禁忌清单与写前自检，确保 LLM 调用时遵循固定决策路径。