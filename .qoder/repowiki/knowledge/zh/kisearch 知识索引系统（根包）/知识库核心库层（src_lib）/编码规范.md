- 跨子模块共享的标识符（如 SERVICE_NAME、DEFAULT_TAGS、DEFAULT_PARTITION_CONFIG）集中定义于 constants.ts，禁止在各子模块重复声明
- kb 运行时目录通过 getKbBaseDir() 延迟读取 config.dataDir，避免在模块加载阶段强依赖配置
- 所有对外暴露的 API 以纯函数或类方法形式组织，不持有全局可变状态，便于单元测试与多实例复用
- 错误处理统一通过抛出结构化 Error 并由调用方捕获，子模块内部不吞异常