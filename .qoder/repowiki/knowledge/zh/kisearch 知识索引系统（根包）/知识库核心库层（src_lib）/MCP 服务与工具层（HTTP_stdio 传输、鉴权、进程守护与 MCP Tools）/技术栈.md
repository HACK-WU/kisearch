@modelcontextprotocol/sdk 的 StreamableHTTPServerTransport 作为 MCP 传输协议；Zod 用于工具参数 schema 校验；Node.js 原生 `node:http` 而非 Express；`crypto.timingSafeEqual` 实现常量时间 Token 比较；JSON 文件 + 原子 rename 实现多 Token 存储持久化。