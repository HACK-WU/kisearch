所有测试通过 `node --test test/*.test.ts` 或 `node --test test/**/*.test.mjs` 运行；MCP 相关单测使用 `npx jiti test/mcp-http.test.ts` 直接执行；E2E 端到端手册见 `test/e2e-guide.md`，需先准备 `test/fixtures/mock-wiki` 并修改 `dataDir` 至临时目录；向量化相关用例依赖可选的 memory 服务及 `SILICONFLOW_API_KEY`/`GITNEXUS_EMBEDDING_API_KEY` 环境变量。