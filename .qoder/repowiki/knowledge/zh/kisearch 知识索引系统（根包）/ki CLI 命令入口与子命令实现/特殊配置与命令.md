`bin/ki.mjs` 是全局可执行入口，所有子命令通过 `npx jiti src/<cmd>.ts` 动态加载；可通过任意位置传入 `--config <path>` 设置配置文件路径（注入 `KI_CONFIG_PATH` 环境变量）；`mcp` 命令支持 `--daemon/-d` 后台常驻模式（须与 `--http` 同时出现）；安装与升级使用 `scripts/install-latest.sh`，支持 `--local` 本地模式。