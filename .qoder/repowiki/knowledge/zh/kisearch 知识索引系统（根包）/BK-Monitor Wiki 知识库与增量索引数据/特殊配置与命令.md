增量索引通过 PYTHONPATH 方式运行：`PYTHONPATH=bk-monitor-wiki/scripts python3 -m wiki_incremental.cli build-index --wiki-dir <wiki> --repo-dir . --repo-url <url> --branch <branch>`；变更检测用 `detect --new-commit <hash>`；反查受影响页面用 `lookup --files ...` 或 `lookup --new-commit <hash>`。Django URL 解析脚本用法：`python scripts/django-url-view-resolver.py "<URL>" "<METHOD>"`。