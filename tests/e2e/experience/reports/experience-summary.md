# 动态体验验证报告（外部Wiki直导 + 增量直连）

> requirement_ref: REQ-20260806-001（REQ-01/02/04/06/07/08/12 + §4.4）
> 前置门禁：e2e journey-wiki-direct-import ✅ PASS
> 时间：2026-08-07
> 状态：✅ 完成（2 批 15 体验点，发现并修复 4 项体验问题）
> **体验基线 commit**：`2548d73c3e60266803bf3f43fb41facddc0cc711`（体验执行时 HEAD；体验修复 `import.ts`/`incremental.ts` 为基线后未提交改动）

## 体验结论

| 批次 | 范围 | 体验点 | 通过 | 发现问题 |
|------|------|--------|------|----------|
| Batch1 | 直导链路（负面/边界/正向） | 8 | 7 | 1（skipped 可观测性） |
| Batch2 | 增量直连（幂等/边界） | 7 | 7 | 2（超大跳过静默 + 删除误报） |
| **合计** | | **15** | **14** | **3**（均修复） |

## 修复问题清单

| # | 严重度 | 问题 | 修复 |
|---|--------|------|------|
| 1 | 🟢 低 | 直导 `stats` 无 `skipped` 计数，跳过文件仅 stderr 日志且与进度条同行混排 | `ImportStats.skipped` + 日志独立行 + 摘要含 skipped |
| 2 | 🟡 中 | 增量超大文件跳过静默（errors 不记录，commit 推进后文件永久不同步且无感知） | 改 `errors.push`（P1 语义，与 challenger 修复的 chunk 超限对齐） |
| 3 | 🟢 低 | 删除"从未导入"文件误报 error（文件本不在库内，删除属无事可做） | 降级 `logWarn` 信息提示（`[delete skip]`） |

## 验证覆盖亮点

**负面路径（错误反馈质量优秀）**：空目录 / 目录不存在 / 缺 root-name / 未知 mode / 未首导增量——全部明确报错含路径与引导。

**幂等性**：同文件多次修改 memoryId 每次更新、旧向量清理；无变更增量明确显示 `0/0/0` 非静默。

**边界正确性**：大文件跳过可观测；文件重命名 R→deleted+added 无残留；增量后检索新内容召回旧内容消失。

## 回归保障

- 全量测试 **340/340 全绿**
- incremental-direct 3/3、chunker 10/10
- lint 零错误

## 副作用

- 体验测试数据（/tmp/ki-exp-*）已全部清理 ✅
